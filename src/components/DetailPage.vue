<template>
  <div>
    <p>{{ $route.params.id }}</p>
    <!-- <h3>{{ post.title }}</h3>
    <span>{{ post.number }}</span>
    <span>{{ post.date }}</span>
    <p>{{ post.content }}</p> -->
  </div>
</template>

<script>
export default {
    props:{
    }
}
</script>

<style>

</style>